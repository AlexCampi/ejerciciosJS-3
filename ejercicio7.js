let numerador = parseInt(window.prompt("Dime el numerador"))
let denominador = parseInt(window.prompt("Dime el denominador"))
denominador === 0
? console.log("No se puede dividir por cero")
: console.log(numerador/denominador)